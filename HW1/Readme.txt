using "g++ -std=c++11 main.cpp" to compile, these code con complier on mac 12.6 and windows10 with devcpp 5.11.0.0
there are 1 .cpp file and 3 .h files in rar file
    main.cpp --it is main file,please compile it
    v1.h -- contain question 1 code
    v2.h -- contain question 2 code
    sp.h -- contain split string code using in v1.h v2.h
main.cpp can change the parameter to select input file and outfile name
using time.h and clock time to calculate time spend
